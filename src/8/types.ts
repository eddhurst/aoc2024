export type Antenna = { row: number; col: number };
export type Antennae = Record<string, Antenna[]>;

export type AntiNode = { row: number; col: number };

export type CitySize = { width: number; height: number };
